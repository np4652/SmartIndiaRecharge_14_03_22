@model IEnumerable<RoundpayFinTech.AppCode.Model.Team>
@if (Model != null && Model.Any())
{
    int i = 0;
    foreach (var item in Model)
    {
        i++;
        <tr>
            <td>@i</td>
            <td>@item.Name</td>
            <td>@item.MobileNo</td>
        </tr>
    }
}