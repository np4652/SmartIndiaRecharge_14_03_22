@model RoundpayFinTech.AppCode.Model.ROffer.SubMplanRofferResp
<div class="row">
    <div class="col-md-12">
        <button type="button" class="close" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <h3>R OFFER</h3>
        <hr />
    </div>
    @if (Model.Records != null)
    {
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="scroll-content">
            <table class="table table-striped">
                <thead class="bg-tableth">
                    <tr>
                        <th>RS</th>
                        <th>Description</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Records)
                    {
                    <tr>
                        <td><span class="btn btn-outline-success" id="spnOferAmt_@item.RS" style="margin-right: 0.5rem;">@item.RS</span></td>
                        <td>@item.Desc</td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    }
    else
    {
    <h1 class="text-danger">Oops no any roffer found!</h1>
    }
</div>
