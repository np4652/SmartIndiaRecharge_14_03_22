@{
    ViewData["Title"] = "Status";
}

@section scripts{
     if (queryString && queryString.order_id !== '' && queryString.order_token !== '') {
            alert('success');
            //window.close();
        }
}